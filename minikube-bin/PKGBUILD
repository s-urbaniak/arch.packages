#Maintainer sur <sergiusz.urbaniak@gmail.com>

pkgname=minikube-bin
_pkgname=minikube
pkgver=0.20.0
pkgrel=1
pkgdesc="Minikube is a tool that makes it easy to run Kubernetes locally."
arch=('x86_64')
url='https://github.com/kubernetes/minikube'
licence=('MIT')
provides=("minikube=${pkgver}")
conflicts=('minikube')
source_x86_64=("https://github.com/kubernetes/minikube/releases/download/v${pkgver}/${_pkgname}-linux-amd64")
md5sums_x86_64=('a8f2db3c87768d9be307297a38e1f71e')

_PKGARCH=amd64

package() {
    cd $srcdir
    install -Dm755 "$_pkgname-linux-amd64" "${pkgdir}/usr/bin/${_pkgname}"
}
